{
  "scripts": {
    "clean": "rm -rf lib",
    "dev": "npm run clean && rollup -c --environment NODE_ENV:development",
    "build": "npm run clean && NODE_ENV=production && rollup -c --environment NODE_ENV:production",
    "deploy": "git add . && git commit -m 'rollup' && git push origin master"
  },
  "devDependencies": {
    "@babel/core": "7.3.3",
    "@babel/preset-env": "7.3.1",
    "babel-core": "7.0.0-bridge.0",
    "babel-preset-latest": "6.24.1",
    "rollup-plugin-babel": "4.3.2",
    "rollup-plugin-uglify": "6.0.2"
  }
}
